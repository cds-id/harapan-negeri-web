// Export all loader-related hooks and providers
export {
  GlobalLoaderProvider,
  useGlobalLoader,
  useSimpleLoader,
  useAsyncLoader,
  useRouteLoader,
} from './useGlobalLoader';

// Re-export the types if needed in the future
export type {
} from './useGlobalLoader';
